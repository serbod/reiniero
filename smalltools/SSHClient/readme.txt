Lazarus/FPC demonstration SSH program.

You can use 
- username/password authentication or
- use a private key in openssh format (specify any passphrase in the password property). Passphrases are untested

Uses Synapse and libssh. Windows libssh dll included for convenience.
Please see respective files for licensing information.